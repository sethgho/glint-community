{
  "name": "glint-community",
  "version": "0.1.0",
  "description": "Community gallery and registry for Glint emotion styles",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.3",
    "hono": "^4.7.12",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
